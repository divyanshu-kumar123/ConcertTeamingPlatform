const User = require('./user.model');
const Otp = require('./otp.model');
const Team = require('./team.model');
const Invitation = require('./invitation.model');
const SeatingGroup = require('./seatingGroup.model');

module.exports = {
  User,
  Otp,
  Team,
  Invitation,
  SeatingGroup,
};
